export function DataTable({headers,rows}:{headers:string[];rows:string[][]}){return <div className="overflow-x-auto card"><table className="w-full text-sm"><thead><tr>{headers.map(h=><th className="p-2 text-left" key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i} className="border-t">{r.map((c,j)=><td className="p-2" key={j}>{c}</td>)}</tr>)}</tbody></table></div>}
